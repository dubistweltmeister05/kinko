[[RhyGen]]

Here is my issue. I want you to refer the way CAN PAL has been written. In the I2C pal, there are some issues that I have. I want the pipelines to be as follows, regardless of the I2C device Being Master or Slave -

Tx -> The app should put data that is to be sent into the allocated Tx RingBuffer, and then it should now worry about the way that it is sent. 
Rx -> Any received data should be put into the allocated Rx Ring buffer that the application has access to. 

I want you to evaluate the use of HAL_I2C_Slave_Sequential_Receive_IT, HAL_I2C_Slave_Sequential_Transmit_IT, HAL_I2C_Master_Sequential_Receive_IT, and HAL_I2C_Master_Sequential_Transmit_IT; tell me when is the interrupt generated by these functions - is it after a single byte tx/rx or is it after the requested length has been sent/received. 

Now, I can see that the message that is being sent via the app is statically allocated, then passed to the PAL, which calls HAL tx. What I want to do, is that the app shall simply push data to the ringbuffer, and the process of transmitting needs to be handled via the PAL. NOT via simple handoff to the HAL, but via the ringubffers. 

For the rx, I want to simply read from the ringbuffers.

Now, is something like this even possible for I2C? 